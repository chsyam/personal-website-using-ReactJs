import Header from "../Header";
import Footer from "../Footer";
import CirtificationsList from "./CirtificationsList"

const Accomplishments = () => {
    return (
        <div>
            <Header />
            <CirtificationsList />
            <Footer />
        </div>
    );
}

export default Accomplishments;